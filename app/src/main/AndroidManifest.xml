<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppStarterKit"
        android:usesCleartextTraffic="false">

        <!-- Startup Initializers -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            android:multiprocess="false">

            <meta-data
                android:value="androidx.startup"
                android:name="com.example.appstarterkit.startup.TimberInitializer" />

            <!--<meta-data
                android:value="androidx.startup"
                android:name="com.example.appstarterkit.startup.WorkManagerInitializer" />-->

            <meta-data
                android:value="androidx.startup"
                android:name="com.example.appstarterkit.startup.RoomInitializer" />

            <meta-data
                android:value="androidx.startup"
                android:name="com.example.appstarterkit.startup.DataStoreInitializer" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppStarterKit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
